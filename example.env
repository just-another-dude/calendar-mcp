# Google OAuth 2.0 Client Credentials (from Google Cloud Console)
# Get these by creating OAuth 2.0 Credentials for 'Web app' (for Railway deployment)
GOOGLE_CLIENT_ID='YOUR_GOOGLE_CLIENT_ID_HERE'
GOOGLE_CLIENT_SECRET='YOUR_GOOGLE_CLIENT_SECRET_HERE'

# Path to the file where the user's OAuth tokens will be stored
# For Railway: Use /app/tokens/saved-tokens.json (automatically set)
# For local: Use .gcp-saved-tokens.json
TOKEN_FILE_PATH='.gcp-saved-tokens.json'

# Port for the local webserver used during the OAuth callback
# For Railway deployment, this should match your Railway app URL port
OAUTH_CALLBACK_PORT=8080

# Google Calendar API Scopes (adjust if necessary)
# https://developers.google.com/calendar/api/guides/auth
# This scope allows reading and writing events
CALENDAR_SCOPES='https://www.googleapis.com/auth/calendar'
# Use 'https://www.googleapis.com/auth/calendar.readonly' for read-only access

# --- Server Configuration ---
# Host and port settings (Railway overrides these automatically)
HOST='127.0.0.1'  # Railway uses 0.0.0.0 automatically
PORT=8000          # Railway provides PORT environment variable

# Development settings
RELOAD='true'      # Disabled automatically in Railway production
NODE_ENV='development'  # Set to 'production' for production deployment

# --- Webhook Configuration ---
# Optional secret key for webhook validation
WEBHOOK_SECRET_KEY='your_webhook_secret_key_here'

# --- Railway-Specific Environment Variables ---
# These are automatically set by Railway, but documented here for reference:
# RAILWAY_ENVIRONMENT=production  # Set by Railway
# PORT=<dynamic>                  # Set by Railway

# --- OpenAI Integration (Optional) ---
# For webhook forwarding to OpenAI Platform
# OPENAI_API_KEY='your_openai_api_key_here'
# OPENAI_WEBHOOK_ENDPOINT='https://your-openai-webhook-endpoint.com'

# --- Google Cloud Console OAuth Setup for Railway ---
# When setting up OAuth credentials for Railway deployment:
# 1. Go to Google Cloud Console > APIs & Services > Credentials
# 2. Create OAuth 2.0 Client ID for 'Web application'
# 3. Add your Railway app URL to 'Authorized JavaScript origins':
#    https://your-app-name.up.railway.app
# 4. Add callback URL to 'Authorized redirect URIs':
#    https://your-app-name.up.railway.app/oauth2callback
#    http://localhost:8080/oauth2callback (for local testing)